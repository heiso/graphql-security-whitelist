{
  "name": "@example/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w --pretty --preserveWatchOutput",
    "dev": "node --inspect=0.0.0.0:9229 --enable-source-maps --no-lazy ./dist/src/app.js"
  },
  "dependencies": {
    "@graphql-security-whitelist/apollo-server-plugin": "*",
    "apollo-server": "^3.4.0",
    "graphql": "^16.0.0"
  }
}
